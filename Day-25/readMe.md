# longest substring without extra character
- in this problem string contain spces numbers  and aslo simbols so approch to solve this problem is 
- use freq array to store charter count if duplicate charter then we remove until freq is one we use slding windo aporch duplicate char is found then we move left tilcome to right 